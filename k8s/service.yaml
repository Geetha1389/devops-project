apiVersion: v1
kind: Service

metadata:
  name: springboot-service

spec:
  type: NodePort

  selector:
    app: springboot

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
